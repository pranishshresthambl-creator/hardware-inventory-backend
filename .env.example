# PostgreSQL Database Configuration for Docker
POSTGRES_DB=django_db
POSTGRES_USER=django_user
POSTGRES_PASSWORD=django_pass
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Set to true if you want to force PostgreSQL usage locally
USE_POSTGRES=true
